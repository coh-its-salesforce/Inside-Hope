<aura:component implements="flexipage:availableForAllPageTypes,force:hasRecordId" access="global">
    <aura:attribute name="recordId" type="String"/>
    <aura:attribute name="disabled" type="Boolean" default="false" />
    <aura:attribute name="saved" type="Boolean" default="false" />
    <aura:attribute name="showSpinner" type="Boolean" default="true" />
    <aura:if isTrue="{!v.showSpinner}">
        <lightning:spinner />
    </aura:if>
    <aura:if isTrue="{!!v.saved}">
        <lightning:card iconName="standard:Campus_Plan_FAQ__c" title="">
        <lightning:recordEditForm
            aura:id="createFAQForm"
            onload="{!c.handleLoad}"
            onsubmit="{!c.handleSubmit}"
            onsuccess="{!c.handleSuccess}"
            recordId="{!v.recordId}"
            objectApiName="Campus_Plan_FAQ__c">
            
            <lightning:messages/>
                
                <lightning:inputField fieldName="Question__c"/>
                <lightning:inputField fieldName="Answer__c" />
                <lightning:inputField fieldName="FAQ_Group__c" />
            	<lightning:inputField fieldName="FAQ_Group1__c" />
                <lightning:inputField fieldName="Visible__c" />
                
            <div class="slds-m-top_medium"><center>
                <lightning:button disabled="{!v.disabled}" variant="brand" type="submit" name="save" label="Save" />
                <lightning:button variant="Neutral" name="Cancel" label="Cancel" /></center>
            </div>
        </lightning:recordEditForm> </lightning:card>
        <aura:set attribute="else">
            <p>Saved!</p>
        </aura:set>
    </aura:if>
</aura:component>